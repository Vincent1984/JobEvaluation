# UI分类树显示修复

## 🐛 问题
UI创建第一层级时报错：`name 'display_tree' is not defined`

## 🔍 原因
之前为了避免expander嵌套问题，将函数名从 `display_tree` 改为 `display_tree_flat`，但忘记更新函数调用处。

## ✅ 修复
更新了两处函数调用：
1. 递归调用子分类：`display_tree(node['children'], level + 1)` → `display_tree_flat(node['children'], level + 1)`
2. 初始调用：`display_tree(tree_data)` → `display_tree_flat(tree_data)`

## 📝 修改位置
文件：`src/ui/app.py`
- 第1444行：递归调用
- 第1446行：初始调用

## ✅ 验证
- 语法检查通过
- 函数名称一致
- 递归逻辑正确

## 💡 使用说明
现在可以正常：
1. 选择企业
2. 创建第一层级分类
3. 查看分类树
4. 创建第二、三层级分类
5. 添加标签和样本JD

刷新浏览器即可使用修复后的功能！
